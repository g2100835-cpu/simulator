width: 1 + Math.abs(mm.weight) * 3 });
                });
                resetPanel();
                return;
            }

            selectedEdgeId = eid;
            dimAllEdges();
            highlightEdge(eid);
            showEdgeInfo(eid);
        });
    })();
    </script</body></html>